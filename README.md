# LIFF Friend Invite Share

這是一個 LIFF 範例專案。

功能很單純：檢查 `shareTargetPicker` 是否可用，可用時分享一則帶有官方帳號加入連結的訊息；不支援時關閉 LIFF。

## 安裝

```bash
pnpm install
```

## 開發

```bash
pnpm dev
```

## 建置

```bash
pnpm build
```
